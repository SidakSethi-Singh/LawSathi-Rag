# Pinned dependency baseline for reproducible benchmark results.
#
# The benchmark's scores depend on the exact versions of the retrieval/scoring
# libraries (sentence-transformers embeddings, chromadb index format, ragas
# metrics), so these are pinned rather than floating (`>=`). Validate this set
# against the environment that produced the published (DOI) results and adjust
# if needed — the CI workflow installs this file, so incompatibilities surface
# on PRs.
openai==1.51.0
sentence-transformers==3.0.1
rank-bm25==0.2.2
chromadb==0.5.5
tiktoken==0.7.0
ragas==0.1.21
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
matplotlib==3.9.2
python-dotenv==1.0.1
tqdm==4.66.5
